# dsp-ecg-processing-system
This project involves modeling a digital signal processing (DSP) system in MATLAB for ECG signal analysis, including noise filtering and heartbeat detection. It also includes the design of a finite state machine (FSM) to represent the control logic of a hardware system managing the signal processing pipeline.
